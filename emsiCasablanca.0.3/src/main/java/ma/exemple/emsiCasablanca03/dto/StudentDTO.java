package ma.exemple.emsiCasablanca03.dto;

import jakarta.validation.constraints.NotEmpty;
import jakarta.validation.constraints.Pattern;
import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;
/*
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
public class StudentDTO {
    private Long id;

    @NotEmpty(message = "Please enter your firstname.")
    private String firstname;
    @NotEmpty(message = "Please enter your lastname.")
    private String lastname;
    @Pattern(regexp = "^[a-zA-Z0-9._%+-]+@emsi-edu\\.ma$", message = "Please enter a valid @emsi-edu.ma email address.")
    private String email;

    @Pattern(
            regexp = "^(?=.*[0-9])(?=.*[a-z])(?=.*[A-Z])(?=.*[@#$%^&+=]).{8,20}$",
            message = "Password must be 8-20 characters long, contain at least one digit, one lowercase letter, one uppercase letter, and one special character")
    private String password;
    private String phone;
    private String sector;
    private String group;
    private String level;
}

 */
